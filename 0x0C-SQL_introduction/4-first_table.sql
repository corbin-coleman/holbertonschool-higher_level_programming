-- Create a table in a database on a MySQL server
-- Database is passed to mysql as an argument, don't create the table if it already exists
CREATE TABLE IF NOT EXISTS first_table (id INT, name VARCHAR(256));
